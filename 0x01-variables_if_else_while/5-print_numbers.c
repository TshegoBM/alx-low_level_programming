#include <stdio.h>
/**
 * main - Entry point
 * Description: Print numbers 1 - 10
 * Return: 0 (success)
 */
int main(void)
{
	char i = 48;

	while (i <= 57)
	{
		putchar(i);
		i++;
	}

	putchar('\n');
	return (0);
}
