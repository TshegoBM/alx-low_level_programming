#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @a: characher to be checked
 * Return: a
 */
int _abs(int a)
{
	if (a < 0)

	a = -a;

	if (a == 0)
	
	else

	_putchar(a);

	return (a);
}
