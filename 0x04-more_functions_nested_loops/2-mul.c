#include "main.h"
/**
 * mul - Multiplies two integers.
 * @a: Integer to be multiplied.
 * @b: Integer to be multiplied.
 * Return: result (a*b)
 */
 int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
